#配置服务器信息
server:
  port: 8080

spring:
  application:
    name: pd-chat
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
  aop:
    proxy-target-class: true

viatris:
  knife4j:
    enable: true
  boot:
    check:
      enable: false

